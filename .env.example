# LLM Configuration
# -----------------
# Provider: "openai" or "lmstudio" (default: lmstudio)
LLM_PROVIDER=lmstudio

# OpenAI settings (when LLM_PROVIDER=openai)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini

# LMStudio settings (when LLM_PROVIDER=lmstudio)
LMSTUDIO_BASE_URL=http://localhost:1234/v1

# Embedding Configuration
# -----------------------
# Provider: "openai", "lmstudio", or "kusto" (default: lmstudio)
EMBEDDING_PROVIDER=lmstudio

# OpenAI embedding settings (when EMBEDDING_PROVIDER=openai)
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# LMStudio embedding settings (when EMBEDDING_PROVIDER=lmstudio)
LMSTUDIO_EMBEDDING_MODEL=text-embedding-nomic-embed-text-v1.5

# Kusto embedding settings (when EMBEDDING_PROVIDER=kusto)
# KUSTO_EMBEDDING_MODEL_URI=your-model-uri

# Kusto Connection
# ----------------
KUSTO_CLUSTER_URI=https://your-cluster.region.kusto.windows.net
KUSTO_DATABASE=your-database
